Your goal is to update nay vulnerable dependencies in this project.


Do the following:

1. Run `npm audit` to find vulnerable installed packages in this project
2. Run `npm audit fix` to fix vulnerable dependencies
3. Run tests and verify the updates didn't break anything.